apiVersion: v1
kind: Service
metadata:
  name: react
  labels:
    app: react
spec:
  type: ClusterIp
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: react